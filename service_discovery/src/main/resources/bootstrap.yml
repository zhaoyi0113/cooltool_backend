server:
  port: 8080

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true

logging:
  level:
    org.springframework.cloud.consul: DEBUGCNyzzhao:spring-cloud-consul-sample yzzhao$ cat src/main/resources/bootstrap.yml
spring:
  application:
    name: testConsulApp